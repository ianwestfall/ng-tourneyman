import { EqualValidatorDirective } from './equal-validator.directive';

describe('EqualValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = true; //new EqualValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
